kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: blogmd
  namespace: blogmd  
  labels:
    app: blogmd
spec:
  host:
    blogmd-blogmd.apps.ko6r5840.eastus.aroapp.io
  to:
    kind: Service
    name: blogmd
    weight: 100
  port:
    targetPort: 4000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
  wildcardPolicy: None
